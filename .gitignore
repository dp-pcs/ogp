# Dependencies
node_modules/

# Build output
# dist/ is rebuilt on publish (prepublishOnly) and on git installs (prepare),
# so it does not need to be tracked.
dist/

# Logs
*.log
daemon.log
tunnel.log
*.tgz
*.zip

# macOS
.DS_Store

# Environment
.env

# aicoe-code local logs
.aicoe-code/

# OGP state directory (test config)
.ogp/

# Process IDs
*.pid
daemon.pid
tunnel.pid

# Test coverage
coverage/
.vitest/
CURRENT_WORK.md

# Local-only security working docs
# (the published security_best_practices_report.md is intentionally tracked;
# the implementation plan stays local until the fixes ship — see B0033-B0037)
security_fix_plan.md

# Generated overview-video output (regenerate via scripts/render-ogp-overview-video.mjs)
/artifacts/

# Dated scratch / daily working notes (root-level only — NOT .agent/memory/)
/memory/

# Superpowers brainstorm scratch (visual companion mockups)
.superpowers/

# Patent disclosures — keep OUT of this public repo (sensitive IP)
patent-disclosures/

# Agent operating context — private scaffolding, NOT part of the OGP product.
# Contains personality rules, operator profile (USER.md), lessons, and memory.
.agent/
.superpowers/
.tmp/

# Synapse org-memory instructions — leak internal infra (tailnet host, team IDs).
# Keep local only.
SOUL.md
scripts/synapse.sh

# LaTeX build artifacts
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.fdb_latexmk
paper/*.fls
paper/*.out
paper/.obsidian/
paper/tmp*.tmp
paper/REVIEW-*.md

# Large binary assets — host via releases, not git.
ogp-assets.zip
OGP_Signal Path.png

# Local agent rule mirrors (point at private .agent/ scaffolding)
.cursorrules
.windsurfrules

# Claude Code local settings (IDE-specific permissions, not project config)
.claude/settings.local.json
