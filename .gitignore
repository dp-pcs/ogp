# Dependencies
node_modules/

# Build output
# Note: dist/ is NOT ignored because it needs to be published to npm

# Logs
*.log
daemon.log
tunnel.log
*.tgz

# macOS
.DS_Store

# Environment
.env

# OGP state directory (test config)
.ogp/

# Process IDs
*.pid
daemon.pid
tunnel.pid

# Test coverage
coverage/
.vitest/
CURRENT_WORK.md

# Local-only security working docs
# (the published security_best_practices_report.md is intentionally tracked;
# the implementation plan stays local until the fixes ship — see B0033-B0037)
security_fix_plan.md
